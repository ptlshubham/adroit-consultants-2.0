RewriteEngine On

# ✅ Redirect everything from adroitconsultants.in to adroitbiz.com
RewriteCond %{HTTP_HOST} ^(www\.)?adroitconsultants\.in$ [NC]
RewriteRule ^(.*)$ https://adroitbiz.com/$1 [L,R=301]

# 👇 These rules below won't matter anymore since the redirect happens above,
# but I'll leave them commented in case you need them later.

# # Redirect /example.html to /example
# RewriteCond %{THE_REQUEST} /([^.]+)\.html [NC]
# RewriteRule ^ /%1 [NC,L,R]

# # Add .html extension if the file exists
# RewriteCond %{REQUEST_FILENAME}.html -f 
# RewriteRule ^ %{REQUEST_URI}.html [NC,L]

# # Custom error pages with full server paths
# ErrorDocument 404 https://adroitconsultants.in//404.html
# ErrorDocument 500 https://adroitconsultants.in//500.html
# ErrorDocument 405 https://adroitconsultants.in//405.html

# # Redirect HTTP www to HTTPS non-www for another domain
# RewriteCond %{HTTPS} off
# RewriteCond %{HTTP_HOST} ^www\.charotarwindowgallery\.com$ [NC]
# RewriteRule ^ https://adroitconsultants.in/%{REQUEST_URI} [L,R=301]

# # Redirect HTTPS www to HTTPS non-www for another domain
# RewriteCond %{HTTPS} on
# RewriteCond %{HTTP_HOST} ^www\.charotarwindowgallery\.com$ [NC]
# RewriteRule ^ https://adroitconsultants.in/%{REQUEST_URI} [L,R=301]
